import { describe, expect, it } from "vitest";

// Proves the toolchain runs. Deleted once real tests exist.
describe("toolchain", () => {
  it("runs a passing test", () => {
    expect(true).toBe(true);
  });
});
